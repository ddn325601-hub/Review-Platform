# 当前启动状态

生成时间：2026-06-24

## 已启动

| 服务 | 地址 | 状态 | 说明 |
| --- | --- | --- | --- |
| 后端起始版 | `http://127.0.0.1:8081` | 已启动 | 使用 `backend/hm-dianping-starter` 和 `local` profile |
| 前端 Nginx | `http://127.0.0.1:8082` | 已启动 | 从课程 Nginx 包解压，端口由 `8080` 改为 `8082` |
| MySQL | `127.0.0.1:3307` | 已启动 | 工作区专用 MySQL，密码 `l798267901` |
| Redis | `127.0.0.1:6380` | 已启动 | 工作区专用 Redis，密码 `l798267901` |

## 日志位置

```text
logs/backend-starter.out.log
logs/backend-starter.err.log
logs/mysql-3307.out.log
logs/mysql-3307.err.log
logs/redis-6380.out.log
logs/redis-6380.err.log
resources/runtime/nginx-dev/nginx-1.18.0/logs
```

## 环境限制

当前工作区专用 Redis 仍是 Windows Redis `3.2.100`，不支持 Redis Stream，因此完整实现版里的异步秒杀订单队列不能完整运行。要复现完整秒杀章节，请使用 Redis 5+，推荐启动仓库根目录的 `docker-compose.yml`。

## 已验证接口

```text
GET http://127.0.0.1:8081/shop-type/list
GET http://127.0.0.1:8082/api/shop-type/list
```

两条链路均返回 `success=true`。
