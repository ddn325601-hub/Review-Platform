# 章节任务卡

把下面任务当成学习打卡表。每完成一项，建议写下“我验证了什么”和“Redis 里出现了什么 key”。

## 0. 项目启动

- [ ] 能访问前端 `http://127.0.0.1:8082`
- [ ] 后端 `http://127.0.0.1:8081` 正常响应
- [ ] MySQL 中存在 `hmdp` 数据库和课程表
- [ ] Redis 能看到登录、缓存相关 key

## 1. 短信登录

- [ ] 调用 `/user/code`，在后端日志看到验证码
- [ ] 确认 Redis 出现 `login:code:{phone}`
- [ ] 调用 `/user/login` 获取 token
- [ ] 确认 Redis 出现 `login:token:{token}` Hash
- [ ] 理解 `RefreshTokenInterceptor` 和 `LoginInterceptor` 分工

## 2. 商户缓存

- [ ] 第一次查询商户走 MySQL
- [ ] 第二次查询同一商户走 Redis
- [ ] 查询不存在商户，观察空值缓存
- [ ] 修改商户后确认缓存删除
- [ ] 对比穿透、互斥锁、逻辑过期三种方案

## 3. 秒杀下单

- [ ] 理解普通下单的一人一单问题
- [ ] 阅读 `seckill.lua` 的原子判断
- [ ] 准备 Redis 5+ 后创建 `stream.orders` 消费者组
- [ ] 使用 JMeter 脚本压测
- [ ] 检查 MySQL 订单表是否没有超卖和重复下单

## 4. 附近商户

- [ ] 执行 `loadShopData` 写入 GEO 数据
- [ ] 调用 `/shop/of/type` 携带经纬度
- [ ] 观察返回数据的 `distance`
- [ ] 理解 Redis 排序后，MySQL 查询为什么要 `ORDER BY FIELD`

## 5. 签到和 UV

- [ ] 调用 `/user/sign`
- [ ] 查看 `sign:{userId}:yyyyMM` 的 bit 位
- [ ] 调用 `/user/sign/count`
- [ ] 手动推演连续签到统计的右移逻辑
- [ ] 运行 `testHyperLogLog` 理解 UV 估算

## 6. 关注、点赞和 Feed

- [ ] 关注用户后观察 `follows:{userId}`
- [ ] 查询共同关注，理解 Set 交集
- [ ] 点赞笔记后观察 `blog:liked:{blogId}`
- [ ] 发布笔记后观察粉丝的 `feed:{userId}`
- [ ] 调用关注流接口，理解滚动分页参数 `max` 和 `offset`

